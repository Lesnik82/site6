services:
  - type: web
    name: webrtc-signal
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node signal-server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
